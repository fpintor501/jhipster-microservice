apply plugin: "war"

bootWar {
    mainClassName = "com.hc.microservice.MicroserviceApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
